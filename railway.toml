# Railway config: use Dockerfile
[build]
dockerfilePath = "fkapi/Dockerfile"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:8000 --workers 4 --timeout 120 fkapi.wsgi:application"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
